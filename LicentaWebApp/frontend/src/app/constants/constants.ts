
export enum Roles {
    Admin = "Admin",
    User = "User"
}

export enum AccessModifiers {
    Public = "Public",
    Private = "Private"
}

export enum Privilages
{
    Admin = "Admin",
    ViewOnly = "View only",
    Editor = "Editor",
}

export const API_URL: string = "https://localhost:7242"